자바에서 가장 널리 사용되는 Collections
메서드를 통해 요소를 추가, 삭제, 검색, 정렬, 변경할 수 있음

주요 메서드(주요 기능)

E = element
add(E e)                       : 리스트 끝에 요소(값)를 추가
add(int index, E e)            : 특정 인덱스 위치에 요소 추가
remove(int index)              : index 위치한 요소 삭제
remove(Object o)               : 지정된 요소를 삭제(첫번째로 발견된)
remove(Integer.valueOf(정수값)) : index를 모를 때
set(int index, E e)            : 특정 인덱스의 요소를 새로운 값으로 변경
get(int index)                 : 특정 인덱스의 요소를 반환
size()                         : 리스트의 크기
isEmpty()          ::boolean   : 리스트가 비어있는지 확인
contains(Object o) ::boolean   : 리스트에 요소가 포함되어 있는지 확인
indexOf(Object o)              : 첫번째로 위치한 요소의 index를 반환
clear()            ::boolean   : list의 모든 요소 삭제
addAll(Collection c)           : 다른 컬렉션의 모든 요소를 복사해서 추가
subList(int start, int end)    : start ~ end까지의 요소들을 추출해서 새로 만듬
